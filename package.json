{
  "private": true,
  "name": "cloudflare-serve",
  "version": "1.0.0",
  "description": "Serve files with correct MIME types using Cloudflare Workers",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "author": "ian",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.11.1",
    "prettier": "^3.4.2",
    "vitest": "3.2",
    "wrangler": "^4.54.0"
  },
  "dependencies": {
    "mime": "^4.1.0"
  }
}
